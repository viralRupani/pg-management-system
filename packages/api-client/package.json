{
  "name": "@pg/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed fetch client for the PG Management API. Framework-agnostic; consumed by admin (Next.js) and mobile (Expo) via transpilePackages. Ships TS source.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pg/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
