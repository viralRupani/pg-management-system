# Base URL of the NestJS API. The browser calls this directly (CORS must allow
# this app's origin — add http://localhost:3001 + the prod web origin to the API
# enableCors allowlist). At deploy time, set this to the prod API URL.
NEXT_PUBLIC_API_URL=http://localhost:4000
