{
  "name": "@pg/landing",
  "version": "0.1.0",
  "private": true,
  "description": "Basera marketing landing page — static, zero-framework Vite build. Deploys to S3 + CloudFront (no SSR).",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3002",
    "build": "vite build",
    "preview": "vite preview --port 3002",
    "gen:assets": "node scripts/gen-assets.mjs",
    "typecheck": "tsc --noEmit || true"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "vite": "^6.0.7"
  }
}
